{
  "name": "clash-verge-rev-mobile",
  "version": "2.4.3",
  "description": "Clash Verge Rev Android Mobile",
  "scripts": {
    "android:dev": "tauri android dev",
    "android:build": "tauri android build",
    "android:build:aarch64": "tauri android build --target aarch64",
    "android:build:armv7": "tauri android build --target armv7",
    "android:build:x86_64": "tauri android build --target x86_64",
    "android:build:universal": "tauri android build --target universal",
    "android:init": "tauri android init",
    "android:sync": "tauri android sync",
    "gradle:clean": "cd app && ./gradlew clean",
    "gradle:build": "cd app && ./gradlew assembleRelease"
  },
  "author": "Clash Verge Rev Team",
  "license": "GPL-3.0-only"
}

